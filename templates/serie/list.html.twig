{% extends 'base.html.twig' %}

{% block body %}

   <section class="series-list">
       {% for serie in series  %}
       <article>

           <a href="{{ path('serie_details',{'id': serie.id}) }}" title="view serie's details">
               <img src="{{ asset('img/posters/serie/' ~serie.poster)}}" alt="{{ serie.name }}" >
           </a>
           {% endfor %}
       </article>

       todo:liste des séries
   </section>


{% endblock %}



